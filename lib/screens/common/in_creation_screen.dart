import 'package:flutter/material.dart';

class InCreationScreen extends StatelessWidget {
  const InCreationScreen({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Not Used'),
      ),
    );
  }
}
